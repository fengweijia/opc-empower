@charset "UTF-8";
/**
 * 流程页面样式
 */
/* OPC赚钱军师 - 全局样式 */
/* 设计规范：苹果式极简 × 赛博工匠感 */
/* ==================== 颜色系统 ==================== */
/* 主色调 */
page {
  /* 背景色 */
  --color-bg-primary: #0D0D0D;
  /* 纯黑背景 */
  --color-bg-card: #1A1A2E;
  /* 深灰蓝卡片 */
  --color-bg-input: #2A2A3E;
  /* 输入框背景 */
  --color-bg-elevated: #2A2A4E;
  /* 提升的卡片 */
  /* 文字色 */
  --color-text-primary: #F5F5F7;
  /* 主要文字 */
  --color-text-secondary: #B0B0B0;
  /* 次要文字 */
  --color-text-muted: #777777;
  /* 弱化文字 */
  /* 强调色 */
  --color-accent: #00E5A0;
  /* 赚钱绿强调 */
  --color-accent-hover: #00CC8E;
  /* 赚钱绿悬停 */
  --color-accent-muted: rgba(0, 229, 160, 0.2);
  /* 赚钱绿弱化 */
  /* 辅助色 */
  --color-gold: #FFD700;
  /* 金色高级感 */
  --color-gold-muted: rgba(255, 215, 0, 0.2);
  --color-red: #FF6B6B;
  /* 警示红紧迫感 */
  --color-red-muted: rgba(255, 107, 107, 0.2);
  --color-blue: #4A90D9;
  /* 信息蓝 */
  --color-purple: #9B59B6;
  /* 紫色点缀 */
  /* 边框色 */
  --color-border: #4A4A6E;
  --color-border-light: #3A3A5E;
  /* 间距 */
  --spacing-xs: 8rpx;
  --spacing-sm: 16rpx;
  --spacing-md: 32rpx;
  --spacing-lg: 48rpx;
  --spacing-xl: 64rpx;
  --spacing-xxl: 96rpx;
  /* 圆角 */
  --radius-sm: 16rpx;
  --radius-md: 24rpx;
  --radius-lg: 32rpx;
  --radius-xl: 48rpx;
  /* 字体 */
  --font-size-xs: 24rpx;
  --font-size-sm: 28rpx;
  --font-size-base: 32rpx;
  --font-size-lg: 36rpx;
  --font-size-xl: 40rpx;
  --font-size-xxl: 48rpx;
  --font-size-xxxl: 64rpx;
  /* 阴影 */
  --shadow-sm: 0 4rpx 16rpx rgba(0, 0, 0, 0.3);
  --shadow-md: 0 8rpx 32rpx rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 16rpx 64rpx rgba(0, 0, 0, 0.5);
  /* 动画时长 */
  --transition-fast: 0.15s;
  --transition-normal: 0.3s;
  --transition-slow: 0.5s;
}
/* ==================== 基础样式 ==================== */
page {
  background-color: var(--color-bg-primary);
  color: var(--color-text-primary);
  font-size: var(--font-size-base);
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}
/* ==================== 容器与布局 ==================== */
.container {
  min-height: 100vh;
  background-color: var(--color-bg-primary);
  padding: 0 var(--spacing-md);
}
.safe-area-top {
  padding-top: env(safe-area-inset-top);
}
.safe-area-bottom {
  padding-bottom: env(safe-area-inset-bottom);
}
/* ==================== 卡片组件 ==================== */
.card {
  background-color: var(--color-bg-card);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-md);
  box-shadow: var(--shadow-sm);
}
.card-highlight {
  background: linear-gradient(135deg, var(--color-bg-card), var(--color-bg-elevated));
  border: 2rpx solid var(--color-accent);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-md);
}
/* ==================== 按钮样式 ==================== */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: var(--spacing-md) var(--spacing-lg);
  border-radius: var(--radius-sm);
  font-size: var(--font-size-base);
  font-weight: 500;
  text-align: center;
  transition: all var(--transition-fast);
  border: none;
  outline: none;
}
.btn:active {
  transform: scale(0.95);
}
/* 主要按钮 - 赚钱绿 */
.btn-primary {
  background-color: var(--color-accent);
  color: #000000;
}
.btn-primary:active {
  background-color: var(--color-accent-hover);
}
/* 次要按钮 - 透明边框 */
.btn-secondary {
  background-color: transparent;
  border: 2rpx solid var(--color-accent);
  color: var(--color-accent);
}
/* 禁用按钮 */
.btn-disabled {
  background-color: #4A4A4A;
  color: #777777;
  cursor: not-allowed;
}
/* 文本按钮 */
.btn-text {
  background: none;
  border: none;
  color: var(--color-accent);
  padding: var(--spacing-sm);
}
/* ==================== 输入框样式 ==================== */
.input {
  background-color: var(--color-bg-input);
  border: 2rpx solid var(--color-border);
  border-radius: var(--radius-sm);
  padding: var(--spacing-md) var(--spacing-md);
  font-size: var(--font-size-base);
  color: var(--color-text-primary);
  width: 100%;
  box-sizing: border-box;
  transition: border-color var(--transition-fast);
}
.input::-moz-placeholder {
  color: var(--color-text-muted);
}
.input::placeholder {
  color: var(--color-text-muted);
}
.input:focus {
  outline: none;
  border-color: var(--color-accent);
  box-shadow: 0 0 0 2rpx var(--color-accent);
}
.input-area {
  min-height: 200rpx;
  resize: none;
}
/* ==================== 标签样式 ==================== */
.tag {
  display: inline-block;
  padding: var(--spacing-xs) var(--spacing-md);
  border-radius: var(--radius-md);
  font-size: var(--font-size-xs);
  font-weight: 500;
}
/* 赚钱绿标签 */
.tag-accent {
  background-color: var(--color-accent-muted);
  color: var(--color-accent);
}
/* 金色标签 */
.tag-gold {
  background-color: var(--color-gold-muted);
  color: var(--color-gold);
}
/* 警示红标签 */
.tag-red {
  background-color: var(--color-red-muted);
  color: var(--color-red);
}
/* ==================== 进度指示器 ==================== */
.dots {
  display: flex;
  justify-content: center;
  gap: var(--spacing-sm);
}
.dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background-color: var(--color-border);
  transition: background-color var(--transition-normal);
}
.dot.active {
  background-color: var(--color-accent);
}
/* ==================== 呼吸灯效果 ==================== */
.breathing-line {
  height: 2rpx;
  background: linear-gradient(90deg, transparent, var(--color-accent), transparent);
  animation: breathing 2s ease-in-out infinite;
}
@keyframes breathing {
  0%, 100% {
    opacity: 0.3;
  }
  50% {
    opacity: 1;
  }
}
/* ==================== 文字间距加宽 ==================== */
.text-spacing-wide {
  letter-spacing: 0.2em;
}
/* ==================== 工具类 ==================== */
.text-center {
  text-align: center;
}
.text-left {
  text-align: left;
}
.text-right {
  text-align: right;
}
.text-primary {
  color: var(--color-text-primary);
}
.text-secondary {
  color: var(--color-text-secondary);
}
.text-muted {
  color: var(--color-text-muted);
}
.text-accent {
  color: var(--color-accent);
}
.text-gold {
  color: var(--color-gold);
}
.text-red {
  color: var(--color-red);
}
.text-sm {
  font-size: var(--font-size-sm);
}
.text-base {
  font-size: var(--font-size-base);
}
.text-lg {
  font-size: var(--font-size-lg);
}
.text-xl {
  font-size: var(--font-size-xl);
}
.text-xxl {
  font-size: var(--font-size-xxl);
}
.font-bold {
  font-weight: 700;
}
.font-medium {
  font-weight: 500;
}
.mt-sm {
  margin-top: var(--spacing-sm);
}
.mt-md {
  margin-top: var(--spacing-md);
}
.mt-lg {
  margin-top: var(--spacing-lg);
}
.mb-sm {
  margin-bottom: var(--spacing-sm);
}
.mb-md {
  margin-bottom: var(--spacing-md);
}
.mb-lg {
  margin-bottom: var(--spacing-lg);
}
.flex {
  display: flex;
}
.flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
}
.flex-between {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.flex-col {
  display: flex;
  flex-direction: column;
}
.w-full {
  width: 100%;
}
.h-full {
  height: 100%;
}
/* ==================== 页面过渡动画 ==================== */
.page-enter {
  animation: pageEnter 0.3s ease-out;
}
@keyframes pageEnter {
  from {
    opacity: 0;
    transform: translateX(40rpx);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}
/* ==================== 骨架屏 ==================== */
.skeleton {
  background: linear-gradient(90deg, var(--color-bg-card) 25%, var(--color-bg-input) 50%, var(--color-bg-card) 75%);
  background-size: 200% 100%;
  animation: skeleton-loading 1.5s infinite;
  border-radius: var(--radius-sm);
}
@keyframes skeleton-loading {
  0% {
    background-position: 200% 0;
  }
  100% {
    background-position: -200% 0;
  }
}
.process-page {
  min-height: 100vh;
  background-color: var(--color-bg-primary);
  display: flex;
  flex-direction: column;
}
.page-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--spacing-md) var(--spacing-lg);
  padding-top: calc(var(--spacing-md) + env(safe-area-inset-top));
  background-color: var(--color-bg-primary);
  border-bottom: 2rpx solid var(--color-border);
  position: sticky;
  top: 0;
  z-index: 100;
}
.back-btn {
  color: var(--color-accent);
  font-size: var(--font-size-base);
}
.page-title {
  font-size: var(--font-size-lg);
  font-weight: 600;
  color: var(--color-text-primary);
}
.placeholder {
  width: 120rpx;
}
.page-content {
  flex: 1;
  padding: var(--spacing-lg);
}
.process-selection {
  padding: var(--spacing-md) 0;
}
.section-title {
  font-size: var(--font-size-xl);
  font-weight: 700;
  color: var(--color-text-primary);
  margin-bottom: var(--spacing-xl);
  display: block;
}
.process-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--spacing-md);
}
.process-card {
  background-color: var(--color-bg-card);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  transition: all var(--transition-fast);
}
.process-card:active {
  transform: scale(0.97);
  border-color: var(--color-accent);
}
.process-icon {
  font-size: 80rpx;
  margin-bottom: var(--spacing-md);
}
.process-name {
  font-size: var(--font-size-lg);
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: var(--spacing-xs);
}
.process-desc {
  font-size: var(--font-size-sm);
  color: var(--color-text-secondary);
}
.process-result {
  height: calc(100vh - 400rpx);
}
.result-scroll {
  height: 100%;
}
.result-card {
  background-color: var(--color-bg-card);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-lg);
}
.result-title {
  font-size: var(--font-size-lg);
  font-weight: 700;
  color: var(--color-accent);
  display: block;
  margin-bottom: var(--spacing-md);
}
.result-content {
  font-size: var(--font-size-base);
  color: var(--color-text-primary);
  line-height: 1.8;
  white-space: pre-wrap;
  display: block;
  margin-bottom: var(--spacing-lg);
}
.steps-section {
  background-color: var(--color-bg-input);
  border-radius: var(--radius-md);
  padding: var(--spacing-md);
  margin-bottom: var(--spacing-lg);
}
.steps-title {
  font-size: var(--font-size-base);
  font-weight: 600;
  color: var(--color-gold);
  display: block;
  margin-bottom: var(--spacing-md);
}
.step-item {
  display: flex;
  align-items: flex-start;
  gap: var(--spacing-md);
  margin-bottom: var(--spacing-md);
}
.step-number {
  background-color: var(--color-accent);
  color: #000;
  width: 48rpx;
  height: 48rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--font-size-sm);
  font-weight: 600;
  flex-shrink: 0;
}
.step-text {
  font-size: var(--font-size-sm);
  color: var(--color-text-primary);
  line-height: 1.6;
  flex: 1;
}
/* 7天发售界面定制样式 */
.launch-container {
  display: flex;
  flex-direction: column;
  height: 100%;
}
.days-tab {
  white-space: nowrap;
  margin-bottom: var(--spacing-lg);
}
.days-tab .days-tab-inner {
  display: flex;
  gap: var(--spacing-sm);
  padding-bottom: var(--spacing-xs);
}
.days-tab .day-item {
  background-color: var(--color-bg-card);
  border-radius: var(--radius-md);
  padding: var(--spacing-sm) var(--spacing-md);
  min-width: 200rpx;
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  border: 2rpx solid transparent;
  transition: all var(--transition-fast);
}
.days-tab .day-item.active {
  background-color: rgba(0, 229, 160, 0.1);
  border-color: var(--color-accent);
}
.days-tab .day-item.active .day-name, .days-tab .day-item.active .day-theme {
  color: var(--color-accent);
}
.days-tab .day-name {
  font-size: var(--font-size-base);
  font-weight: bold;
  color: var(--color-text-primary);
  margin-bottom: 8rpx;
}
.days-tab .day-theme {
  font-size: var(--font-size-xs);
  color: var(--color-text-secondary);
}
.phone-preview {
  background-color: #000000;
  border-radius: var(--radius-lg);
  border: 8rpx solid var(--color-bg-input);
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  margin-bottom: var(--spacing-lg);
}
.phone-preview .phone-header {
  background-color: var(--color-bg-card);
  padding: var(--spacing-sm);
  text-align: center;
  color: var(--color-text-primary);
  font-size: var(--font-size-sm);
  font-weight: bold;
  border-bottom: 2rpx solid var(--color-bg-input);
}
.phone-preview .phone-content {
  flex: 1;
  padding: var(--spacing-md);
  background-color: var(--color-bg-primary);
}
.phone-preview .content-block {
  margin-bottom: var(--spacing-lg);
}
.phone-preview .block-title {
  color: var(--color-text-secondary);
  font-size: var(--font-size-xs);
  margin-bottom: var(--spacing-sm);
  display: block;
}
.phone-preview .moment-card {
  background-color: var(--color-bg-card);
  border-radius: var(--radius-md);
  padding: var(--spacing-md);
  margin-bottom: var(--spacing-sm);
  color: var(--color-text-primary);
  font-size: var(--font-size-sm);
  line-height: 1.6;
}
.phone-preview .moment-card .action-row {
  margin-top: var(--spacing-sm);
  display: flex;
  justify-content: flex-end;
}
.phone-preview .moment-card .action-row .copy-btn {
  color: var(--color-accent);
  font-size: var(--font-size-xs);
}
.phone-preview .chat-bubble {
  background-color: var(--color-bg-input);
  border-radius: var(--radius-md) var(--radius-md) var(--radius-md) 0;
  padding: var(--spacing-md);
  color: var(--color-text-primary);
  font-size: var(--font-size-sm);
  line-height: 1.6;
  margin-right: 20%;
}
.phone-preview .chat-bubble .action-row {
  margin-top: var(--spacing-sm);
  display: flex;
  justify-content: flex-end;
}
.phone-preview .chat-bubble .action-row .copy-btn {
  color: var(--color-accent);
  font-size: var(--font-size-xs);
}
.phone-preview .phone-footer {
  padding: var(--spacing-md);
  background-color: var(--color-bg-card);
  border-top: 2rpx solid var(--color-bg-input);
}
.phone-preview .phone-footer .done-btn {
  background-color: var(--color-accent);
  color: #000000;
  border-radius: var(--radius-sm);
  font-weight: bold;
  border: none;
}
.fallback-text {
  color: var(--color-text-primary);
  font-size: var(--font-size-sm);
  line-height: 1.6;
  white-space: pre-wrap;
}
.back-section {
  text-align: center;
  padding: var(--spacing-lg);
}
.back-section .btn {
  width: 400rpx;
}
